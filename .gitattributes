# Normalise line endings: LF in the repo, Windows scripts stay CRLF.
* text=auto eol=lf
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf

# Treat binaries as binary so git never mangles them.
*.png binary
*.jpg binary
*.ico binary
